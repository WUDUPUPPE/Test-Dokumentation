name: Build and Release PDF

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: LaTeX kompilieren
        uses: xu-cheng/latex-action@v3
        with:
          root_file: test.tex

      - name: PDF umbenennen
        run: mv test.pdf Testdokumentation.pdf

      - name: PDF als Artefakt speichern
        uses: actions/upload-artifact@v4
        with:
          name: Testdokumentation-PDF
          path: Testdokumentation.pdf

      - name: GitHub Release erstellen und PDF anhängen
        uses: softprops/action-gh-release@v2
        with:
          name: "Testdokumentation ${{ github.ref_name }}"
          files: Testdokumentation.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
